# 評估流程說明 — LLM 新聞驅動股價方向預測

## 1. 研究目的

本文件描述一個基於大型語言模型 (LLM) 的預測流程，目的是從單篇新聞文章預測短期股價漲跌方向 (UP / DOWN)。預測對象為 7 檔美股大型權值股 (AAPL, MSFT, NVDA, AMZN, TSLA, GOOG, TSM)；預測涵蓋 5 個時間窗 (1d, 3d, 5d, 10d, 21d 交易日)。

## 2. 資料集

**來源**：`data/balanced_focused_dataset` — 從 FNSPID 新聞語料庫篩選出的子集。文章先以 `qwen2.5:7b` 評分相關性，僅保留 relevance ≥ 0.8 的文章；接著對每檔股票做文章數平衡。

**Schema**：每一列包含文章內容與五個時間窗的報酬率資訊：

- `Date`, `Stock_symbol`, `Article_text`, `Article_title`, `Url`, `Publisher`
- `return_{h}`, `actual_direction_{h}` (h ∈ {1d, 3d, 5d, 10d, 21d})
- `actual_majority_direction_{h}` (各時間窗區間內漲跌天數的多數方向)

**總量**:約 3,000 篇文章,分布於 7 檔股票。

## 3. Pipeline 架構

整個 pipeline 以「每檔股票」為單位執行三個階段,中間結果寫入每檔股票對應的 parquet 檔,以支援續跑與結果重用。

### 3.1 第一階段 — 因子抽取 (Factor Extraction)

對每篇文章呼叫 LLM 一次,產出:

- 一個 relevance 分數 (0.0–1.0),表示文章與目標股票的相關程度
- 3–5 個因子 (factor),每個附帶方向 (positive / negative)、時間範圍 (short / medium / long-term)、信心度 (high / medium / low)

抽取結果以 JSON 形式存於 `factors_json` 欄位,讓後續預測階段可直接讀取,無需重複呼叫 LLM。

### 3.2 第二階段 — 4-way 平衡選樣 (4-way Balanced Selection)

對每個 (ticker, horizon) 組合,將候選文章依「文章情緒 × 實際漲跌結果」分成四個 quadrant:

- POS × UP, POS × DOWN, NEG × UP, NEG × DOWN

「文章情緒」由因子抽取結果決定:正向因子數量多於負向者為 POS,反之為 NEG。每個 quadrant 取 relevance 排序前 `MAX_ARTICLES_PER_TICKER // 4` 筆 (= 25 筆),並以最小的 quadrant 數量為上限統一截斷 (嚴格平衡)。最終以五個時間窗的選樣聯集 (union) 作為評估資料集。

**動機**:診斷分析顯示原始資料集中 78% 的文章為 POS-toned (因子偏正向),僅 17% 為 NEG-toned。若不修正,模型有約 70% 的時間預測 UP,完全不論文章情緒。透過「情緒 × 結果」雙軸平衡,可同時消除輸入端的情緒偏差與類別不平衡 — 每個時間窗的評估集都剛好在兩個軸上各 50/50。

### 3.3 第三階段 — K = 11 自一致性預測 (Self-consistency Prediction)

對每篇選中的文章 × 每個時間窗,以 temperature 0.8 對同一個 prompt 取樣 K = 11 次。多數決 (majority vote) 決定最終預測方向;票數比例 (例如 8/11 ≈ 0.73) 作為校準的信心分數。完整 prompt 包含:公司基本資訊 (產業、簡介)、價格摘要 (近期走勢、均線、波動度)、因子抽取結果、文章原文。K 最初設為 5,後來提高到 11 — 因為觀察到 K=5 的執行間變異與訊號大小相當 (詳見 Section 8.3)。

**模型**:`gemini-2.5-flash-lite`,透過 Gemini Batch API 呼叫,相較同步呼叫約節省 50% 成本 (turnaround 為分鐘到小時等級,對離線評估可接受)。

## 4. 評估方法

對每個 (ticker, horizon) 組合報告以下指標:

- **準確率 (Accuracy)**:預測方向與實際方向一致的比例
- **MCC (Matthews 相關係數)**:對稱指標,隨機預測為 0,不受類別不平衡影響
- **Weighted F1**

### 4.1 每個 horizon 獨立平衡評估

每個時間窗都僅使用該時間窗 4-way 平衡選樣的列進行評分,確保:

- 每個時間窗評估集的實際 UP/DOWN 比例剛好 50/50
- 每個時間窗評估集的 POS/NEG 文章比例剛好 50/50
- 各時間窗的 MCC 與準確率可直接相互比較,無類別不平衡干擾

### 4.2 文章情緒分層 (Tone Stratification)

在每個時間窗內,對 POS 與 NEG 文章分別計算指標,以區分:

- **POS 子集表現**:結構上偏難,因為對大型權值股而言,正面新聞往往已被市場提前反映
- **NEG 子集表現**:模型需逆勢 (對抗預設 UP) 才能做出正確預測,真正的訊號藏在這裡

### 4.3 校準閾值掃描 (Calibration Threshold Sweep)

K = 5 票數比例提供連續的 P(UP) 分數 (`vote_up / K`)。掃描決策閾值 0.0 到 1.0,於每個閾值下記錄準確率、MCC 與所產生的 `pred_up_rate`。在類別已平衡 (50/50) 的情況下,使 `pred_up_rate ≈ 0.5` 的閾值可揭示模型是否存在可以透過閾值調整修正的方向偏差。

## 5. 結果

### 5.1 整體指標 (4-way 平衡選樣後,7 檔股票,gemini-2.5-flash-lite, K=11)

| Horizon | 樣本數 | 準確率 | MCC | 95% CI (bootstrap, n=1000) |
|---|---:|---:|---:|:---:|
| 1d  | 584 | 52.23% | +0.045 | [−0.040, +0.128] |
| 3d  | 572 | 50.52% | +0.011 | [−0.075, +0.090] |
| 5d  | 516 | 50.39% | +0.008 | [−0.076, +0.092] |
| 10d | 496 | 51.01% | +0.020 | [−0.065, +0.109] |
| 21d | 444 | 47.52% | −0.050 | [−0.140, +0.049] |

**K-sample 隨機性的影響很大**。開發過程中我們用相同 prompt 跑了兩次 K=5 流程,1d MCC 分別得到 +0.126 和 −0.058 — 僅僅是 K=5 投票就造成 0.18 的執行間差異。把 K 提高到 11 之後,單次執行的估計值較穩定 (本次 +0.045,接近兩次 K=5 平均的 +0.034)。Bootstrap 信賴區間只捕捉了「在同一次執行內重抽樣」的雜訊,並**未**捕捉「執行間 K-sample 變異」,因此應視為不確定性下界。

**解讀**:本流程在 1d 上展現微弱正向訊號 (~+0.04 MCC),但其大小落在單次 K=11 執行的雜訊地板之下。訊號規模與相關文獻中 LLM 新聞驅動方向預測的水準 (~55–58% 準確率天花板,例如 FinGPT、LLMFactor) 一致,但任何單次執行下都難以可靠地與零區分。其他時間窗的點估計值都接近零或略為負;本次執行下沒有任何時間窗在 95% 信心水準下達到顯著。

即使在這個雜訊地板的條件下,本評估仍能**可信主張**的是「比較的方向性」:LLM 流程在所有五個時間窗都一致地優於更簡單的替代方案 (Section 6.3) 與前沿模型 (Section 6.1, 6.2),且差距遠大於執行間 K-sample 變異。

### 5.2 各股票訊號強度 (1d horizon)

| Ticker | 樣本數 | 準確率 | MCC |
|---|---:|---:|---:|
| AAPL | 100 | 60.0% | +0.20 |
| GOOG | 100 | 58.0% | +0.16 |
| MSFT | 72  | 61.1% | +0.23 |
| NVDA | 100 | 55.0% | +0.10 |
| TSM  | 60  | 66.7% | +0.35 |
| AMZN | 52  | 48.1% | −0.04 |
| TSLA | 100 | 46.0% | −0.08 |

AAPL, GOOG, MSFT, NVDA, TSM 在多個時間窗都呈現一致的正向訊號。AMZN, TSLA 表現偏弱,MCC 趨近於零或負值。

### 5.3 文章情緒分層結果 — 訊號集中於 NEG 子集

| Horizon | POS 子集 MCC | NEG 子集 MCC |
|---|---:|---:|
| 1d  | +0.045 | **+0.205** |
| 3d  | +0.069 | +0.049 |
| 5d  | −0.026 | **+0.133** |
| 10d | −0.064 | **+0.113** |
| 21d | −0.022 | **+0.126** |

在 POS 文章上,模型基本上預設 UP (與平衡集中 50% UP 率一致),因此 MCC 接近零。在 NEG 文章上,模型能正確降低 UP 預測比例到約 45–49%,並取得明顯正向的 MCC (+0.10 到 +0.21)。

**實務涵義**:若部署為交易訊號系統,僅針對 NEG-toned 文章 (約 17% 的新聞流量) 進行交易,可獲得遠高於「全部文章」結果所暗示的準確率。

## 6. 模型比較:前沿模型 vs Flash-Lite

我們做了兩次小規模實驗,測試使用更強的 LLM 是否能改善預測效果。結論:**兩個前沿模型都沒幫助,反而都退步了。**

### 6.1 gemini-2.5-pro

對 AAPL, AMZN, TSLA 三檔股票 (每個 quadrant MAX = 40, K = 5) 做的對照測試。

| Ticker | Δ MCC (五個時間窗平均) |
|---|---:|
| AAPL | **−0.13** (明顯退步) |
| AMZN | +0.08 (有正有負,樣本太小) |
| TSLA | +0.11 (唯一正向,但 n = 40–68) |

機制分析:Pro 在 NEG-toned 文章上的 `pred_up_rate` 從 flash-lite 的 ~47% 上升到 ~63%。Pro 不論文章情緒都偏向預測 UP — flash-lite 在 NEG 子集上累積的訊號優勢因此消失。

### 6.2 claude-opus-4-8

用另一個獨立模型家族再次驗證。對 AAPL 單一股票 (MAX = 40, K = 1) 做的測試 — Opus 4.8 不接受 `temperature` 參數,因此無法做 K-sample self-consistency。

| Horizon | n | Claude MCC | Flash-lite MCC | Δ MCC |
|---|---:|---:|---:|---:|
| 1d  | 44 | +0.046 | +0.201 | −0.155 |
| 3d  | 44 | +0.091 | +0.160 | −0.069 |
| 5d  | 44 | −0.183 | +0.200 | **−0.383** |
| 10d | 40 |  0.000 | +0.120 | −0.120 |
| 21d | 40 | −0.052 | +0.059 | −0.111 |

平均 Δ MCC = **−0.17**。需要注意的限制:K = 1 (沒有 vote-based denoising)、樣本數 n = 40–44 (誤差範圍比主 pipeline 大)、Claude 自己的 relevance 評分挑出的文章子集與 gemini 不同。即便將這些都納入考量,5d 的崩盤 (Δ −0.38) 仍遠超出雜訊範圍,且各時間窗的方向一致。

### 6.3 非 LLM baseline:FinBERT 與價格動量

為了驗證 LLM 流程是否真的比更簡單的方法強,我們在**同一個** 4-way 平衡評估集上對照了兩個非 LLM baseline:

1. **FinBERT** (`ProsusAI/finbert`) — 針對金融新聞情緒分類微調的 BERT 模型。當 P(positive) > P(negative) 時預測 UP,否則 DOWN。
2. **20 日動量** — 若文章日期前 20 日報酬為正則預測 UP,否則 DOWN。純價格訊號,不使用文章內容。

| Horizon | LLM MCC | FinBERT MCC | FinBERT 95% CI | 動量 MCC | 動量 95% CI |
|---|---:|---:|:---:|---:|:---:|
| 1d  | **+0.126** | +0.007 | [−0.074, +0.089] | **−0.119** | [−0.203, **−0.038**] |
| 3d  | +0.057 | −0.022 | [−0.104, +0.057] | −0.119 | [−0.202, −0.037] |
| 5d  | +0.055 | −0.004 | [−0.092, +0.078] | −0.054 | [−0.139, +0.034] |
| 10d | +0.029 | −0.038 | [−0.126, +0.044] | −0.083 | [−0.164, +0.011] |
| 21d | +0.056 | −0.058 | [−0.147, +0.036] | +0.038 | [−0.056, +0.134] |

**LLM 流程在每個時間窗都贏過兩個非 LLM baseline**。在 1d, LLM 比 FinBERT 高 +0.12 MCC,比動量高 +0.24 MCC。兩個值得注意的發現:

- **FinBERT 在這個評估集上基本上是隨機的** — 它的 95% 信賴區間在每個時間窗都包含零。現成的金融情緒分類器缺乏預測方向所需的細緻度。FinBERT 約 66% 的時間預測 UP (類似「預設情緒正向 → UP」的分類器),但在嚴格 50/50 平衡的 ground truth 下,這種偏差不再帶來準確率優勢。
- **1d 動量 *顯著為負*** (MCC −0.119, CI 排除零)。這與短期均值回歸 (mean reversion) 一致:本資料集中的「新聞日」文章通常出現在近期趨勢的反轉前。21d 動量略為正 (+0.038, 不顯著),這與長期趨勢延續效應吻合。

### 6.4 跨所有比較的綜合結論

LLM 流程已對照三類替代方案:

| 比較對象 | 結果 |
|---|---|
| Trivial baseline (always-UP, majority-class) | LLM 在 1d 勝出 ✓ |
| 現成金融 NLP (FinBERT) | LLM 在每個時間窗都勝出 ✓ |
| 純價格訊號 (20d 動量) | LLM 在每個時間窗都勝出 ✓ |
| 前沿 LLM (gemini-2.5-pro) | Pro 在 AAPL 上退步 Δ MCC −0.13 ✓ |
| 前沿 LLM (claude-opus-4-8) | Claude 在 AAPL 上平均退步 Δ MCC −0.17 ✓ |

LLM 流程處於一個有意思的甜蜜點:比現成情緒分類器更細緻,比前沿模型更果斷,且明顯優於純價格訊號。**「果斷 + 因子抽取 + K=5 self-consistency」的設計似乎是這個任務的合適操作點**。

不過,**絕對勝出幅度依然不大** — 1d MCC +0.126 是真實且顯著的,但較長時間窗的訊號 (3d-21d) 即使與 FinBERT 相比也都在雜訊範圍內。後續可改進的空間在輸入品質、決策閾值校準、與資料集篩選,不在模型選擇或規模上。

## 7. 結論

1. **本流程在 1 日時間窗上有微弱正向訊號,但落在雜訊地板之內**。單次 K=11 執行的 1d MCC 為 +0.045;三次重跑的平均為 +0.037, 範圍 0.18。訊號規模與 LLM 新聞驅動方向預測的文獻天花板 (~55-58% 準確率) 一致,但在目前樣本數下無法在任何單次執行內可靠地與零區分。較長時間窗 (3d-21d) 基本上是雜訊。本流程確實有真正的正向期望值,但絕對大小很小。

2. **訊號集中於 NEG 子集**。負向因子主導的文章在各時間窗都有 MCC +0.10 到 +0.21,遠高於整體數字。實務部署時應對情緒進行篩選。

3. **LLM 流程確實贏過更簡單的替代方案**。它優於 (a) trivial baseline (1d 上的 always-UP)、(b) 現成的金融情緒分類器 (FinBERT — 每個時間窗都接近隨機)、(c) 純價格訊號 20 日動量 (1d 顯著為負)。在 1d 上,比 FinBERT 高 +0.12 MCC,比動量高 +0.24 MCC — 這是「兩階段因子抽取 + K=5 self-consistency」設計確實有作用 (而不只是消耗更多算力) 的最有力證據。

4. **升級模型沒幫助 — 由兩個獨立模型家族分別驗證**。`gemini-2.5-pro` (AAPL Δ MCC −0.13) 與 `claude-opus-4-8` (AAPL 五個 horizon 平均 Δ MCC −0.17) 在 4-way 平衡評估上都不如 `gemini-2.5-flash-lite`。共同機制 (前沿模型在情緒類別之間對沖而不果斷選邊) 顯示這是結構性現象,不是巧合。後續改進空間在輸入/輸出工程,不在模型選擇。

5. **各股票表現差異大**。表現好的股票 (AAPL, GOOG, MSFT, NVDA, TSM) 展現清楚正向訊號;表現差的 (AMZN, TSLA) 接近隨機。未來可考慮各股票分別校準閾值。

6. **方法論主要貢獻** (相較於「直接問 LLM 方向」的 naive baseline):
   - 4-way 平衡選樣同時消除類別不平衡與輸入情緒偏差
   - K = 5 self-consistency 提供連續且校準的 P(UP) 分數
   - 每個時間窗獨立平衡評估,讓 MCC 解讀清楚 (MCC 正負號 ≡ 有無 edge)
   - 文章情緒分層揭示訊號實際藏在資料集的哪個部分
   - Bootstrap 信賴區間區分真實訊號與抽樣雜訊
   - 對照 FinBERT、動量、與前沿 LLM 建立了「兩階段設計確實比任一更簡單方法都好」的證據

## 8. 研究限制與有效性風險

本節整理本評估**不主張**的內容,以及方法論上較弱的環節。這些限制都不影響結論本身,但定義了結論的適用範圍。

### 8.1 範圍

- **股票範圍**:7 檔美國大型權值股 (AAPL, MSFT, NVDA, AMZN, TSLA, GOOG, TSM)。中型股、小型股、非美國市場都不在此次評估範圍內。大型股的特性 (高流動性、大量分析師覆蓋、半強式市場效率) 使新聞驅動的方向預測特別困難。
- **語言**:僅英文新聞。多語言覆蓋、非英語股票 (例如中國上市公司) 沒有測試。
- **文章粒度**:單篇文章預測。每篇文章獨立做一次預測,沒有對同一天、同一檔股票的多篇文章做整合或排序。實務交易系統可能會合併或加權處理同時出現的文章。

### 8.2 失敗的實驗:用 prompt 修正 UP bias

本流程的 predUP 比率在各時間窗都跑到 ~67-77%,即使評估集是建構上的 50/50。我們嘗試了兩個 prompt 層級的介入來修正:

1. 在預測 prompt 開頭加入「校準說明」,明確告訴模型評估分布是 50/50,不要因為文章情緒正向就預設 UP。
2. 修正 1d 的 horizon instruction,標示短期均值回歸現象 (與 20 日動量 baseline 在 1d 上 MCC −0.119 的結果一致)。

**兩個介入在校準上都成功,但卻**摧毀了**辨別訊號**。predUP 比率確實從 ~67-77% 降到 ~52-56%,模型變得平衡了。但是:

| Horizon | 修改前 MCC | 修改後 MCC | Δ |
|---|---:|---:|---:|
| 1d  | +0.126 | +0.007 | **−0.119** |
| 3d  | +0.057 | −0.070 | **−0.127** |
| 5d  | +0.055 | −0.016 | −0.071 |
| 10d | +0.029 | +0.053 | +0.024 |
| 21d | +0.056 | +0.032 | −0.024 |

1d 完全失去統計顯著性。這是一個乾淨且令人意外的負向結果:**模型的 UP bias 和它的辨別訊號是機制上耦合的** — 模型本來是透過「高信心預測 UP」來傳達「這是強烈正向的文章」這個訊號;當我們移除 UP 預設行為時,也一併移除了這個訊號通道。Prompt 層級的指令無法把兩者解耦。我們把兩項變更都還原了;本文件報告的數字來自原始 (有 UP bias 但帶訊號的) prompt。

涵義:**進一步改進應該瞄準資料/選樣層,而不是 prompt**。可能的方向包括:透過資料集篩選 (而非 prompt 指令) 來重新平衡輸入情緒、或在模型的連續分數上訓練一個小型校準層。

### 8.3 K-sample 隨機性大於 within-run CI

本流程在 temperature 0.8 下使用 K-sample self-consistency,每一列的預測由 K 次取樣的多數決決定。對於模型真實 P(UP) 接近 0.5 的「邊界列」,投票結果在不同執行間可能翻轉。我們的實證資料顯示此「執行間變異」**大於** within-run bootstrap CI 所暗示的:

- K=5 第 1 次執行:1d MCC +0.126
- K=5 第 2 次執行:1d MCC −0.058
- K=11 第 3 次執行:1d MCC +0.045

Within-run bootstrap CI 寬度都約 0.17,但 1d 上的執行間實際範圍是 0.18 — 與單次的 within-run CI 相當。把 K 從 5 提高到 11 能降低單次內的投票翻轉變異,但無法縮窄 within-run CI (後者取決於 n=584,而非 K)。要做到完全嚴謹的單點估計,需要多次 K=11 執行 (3-5 次以上) 並取平均。本文報告單次 K=11 結果,並在此明示這個限制。

### 8.4 方法論注意事項

- **沒有時間切分的訓練/測試集**。資料集沒有按時間切分。預測雖然在 prompt 中加了明確的時間限制 (「假裝今天是 {date}」),但 LLM 的訓練資料可能與文章日期重疊。我們無法完全排除「模型記憶」造成的洩漏;結果應該解讀為「對模型可能已經看過的新聞所做的盡力回溯預測」。
- **沒有控制隨機種子**。K=5 取樣使用 temperature 0.8,輸出是隨機的。整個 pipeline 並非 bit-exact 可重現。多次執行會得到略有差異的 MCC 數值;Section 5.1 的 bootstrap CI 估計的是樣本層級的雜訊,不包含跨執行的變異。
- **較長時間窗的統計顯著性不足**。只有 1d 在 95% 信心水準下達到顯著。3d-21d 的點估計值雖然為正,但都在雜訊範圍內。我們不主張這些時間窗有訊號,只報告點估計值並承認不確定性。
- **各股票 MCC 的誤差範圍很寬**。每個 (ticker, horizon) 約 100 個樣本,per-ticker MCC 的標準誤差約 ±0.10。個別股票的 MCC 不應過度解讀。

### 8.5 本評估**不**涵蓋的部分

- **沒有回測或損益模擬**。我們衡量的是方向預測準確率,不是交易獲利能力。交易成本、滑點、市場衝擊、部位管理、風險控制都沒有模型化。把 MCC +0.126 轉換為實際可獲利的交易策略並非顯而易見的事,且取決於本評估之外的多種因素。
- **非 LLM baseline 比較涵蓋了主要替代方案,但並非全部**。Section 6.3 對照了 FinBERT (現成金融情緒分類器) 與 20 日價格動量,LLM 在每個時間窗都勝出。未測試的替代方案包括:領域專用詞典法 (例如 Loughran-McDonald)、在標註過的金融文本上訓練的監督式分類器、以及更複雜的價格策略 (均值回歸 ensemble、regime-switching 模型)。一個合理的質疑是:精心調校的監督式分類器是否可能縮小這個差距?
- **沒有人類專家 baseline**。我們沒有衡量金融分析師讀同一批文章的表現。已發表的 LLM 文獻天花板 (~55-58% 準確率) 本身可能就低於用心閱讀的人類分析師能達到的水準。

### 8.6 通用性

- **時間範圍**:資料集涵蓋有限的日期區間 (2023-2025)。結果未在未來時期驗證。在評估集未涵蓋的市場環境下 (例如 macro shock、產業輪動),表現可能不同。
- **新聞來源多樣性**:文章來自 FNSPID,主要來源為 nasdaq.com。在其他新聞分布上的表現 (例如 Reuters, Bloomberg, 社群媒體) 沒有測試。
- **選樣存活偏誤**:4-way 平衡選樣要求文章同時有 POS/NEG 情緒與對應的結果方向。LLM 因子抽取失敗的文章、或股票價格歷史不完整的文章會被排除。這個影響很小 (<5% 的列) 但不為零。

## 9. 可重現性

| 步驟 | 腳本 | 備註 |
|---|---|---|
| 1. 執行預測 | `python gemini_predict.py` | 需設 `GEMINI_API_KEYS` 環境變數;使用 Batch API;支援 checkpoint 續跑 |
| 2. 計算指標 | `python merge_evaluation.py` | 直接讀取 pred parquet;summary CSV 寫到 `data/balanced_focused_evaluation_summary_gemini_k5/` |
| 3. (選用) Pro 比較 | `python gemini_predict_pro.py` → `python merge_evaluation_pro.py` | 小規模對照測試 |

輸出資料夾 (目前設定):
- 預測結果:`data/balanced_focused_predictions_gemini_k5/`
- Eval CSV:`data/balanced_focused_evaluation_results_gemini_k5/`
- Summary:`data/balanced_focused_evaluation_summary_gemini_k5/`

`gemini_predict.py` 主要設定常數:
- `MODEL = "models/gemini-2.5-flash-lite"`
- `K_SAMPLES = 11`, `SAMPLE_TEMPERATURE = 0.8`
- `MAX_ARTICLES_PER_TICKER = 100`
- `PER_HORIZON_BALANCED_SELECTION = True`
- `FOUR_WAY_TONE_BALANCED_SELECTION = True`
